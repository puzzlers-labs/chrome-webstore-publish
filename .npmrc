# This file configures settings for Node.js package management tools for this project.
# It specifies the preferred package manager and enforces engine compatibility.

# Sets pnpm version 10.12.4 as the package manager to use for this project.
package-manager=pnpm@10.12.4

# Ensures that only the Node.js and package manager versions specified in package.json are allowed.
# If the versions do not match, installation will fail.
engine-strict=true
# This file specifies files and directories that should be ignored by git version control.
# It helps prevent committing dependencies, logs, OS files, editor settings, and other unnecessary files.
# Keeping the repository clean and free from local or system-specific files.

# Node modules
node_modules/

# Logs generated by pnpm
pnpm-debug.log*

# OS generated files (system-specific metadata)
.DS_Store
Thumbs.db

# VSCode user-specific settings (workspace-shared files are not ignored)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Docker environment overrides and local Docker files
*.local
docker-compose.override.yml

# Swap files created by editors (like Vim)
*.swp
*.swo